import org.junit.jupiter.api.Test;
import org.openqa.selenium.WebDriver;

public class AutoTest {
    WebDriver driver;
    @Test
    void addToCartTest(){
        new LoginPage(driver).emailField
    }
}
